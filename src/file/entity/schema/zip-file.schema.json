{
  "$id": "#/zip-file",
  "title": "ZIP File",
  "type": "object",
  "properties": {
    "name": { "$ref": "#/name" },
    "tags": { "$ref": "#/tags" },
    "meta": { "$ref": "#/file-metadata" },
    "paths": {
      "title": "Paths",
      "description": "Paths in ZIP",
      "type": "array",
      "items": { "$ref": "#/path" }
    },
    "files": {
      "title": "Files",
      "description": "Files in ZIP",
      "type": "array",
      "items": { "$ref": "#/file-ref" }
    },
    "images": {
      "title": "Images",
      "description": "Images in ZIP",
      "type": "array",
      "items": { "$ref": "#/image-file-ref" }
    }
  }
}
